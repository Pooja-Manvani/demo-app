import React from 'react'

/**
 * @name CalculationOverview
 * @param {*} param0 
 * @returns Children component
 */
export default function CalculationOverview({children}) {
  return (
    <div>{children}</div>
  )
}
